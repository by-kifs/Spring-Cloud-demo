spring:
  application:
    name: config-server

  cloud:
    config:
      enabled: true
      server:
        git:
          uri: https://github.com/by-kifs/lasi/
          searchPaths: lasiGekenConfig
          username: ******
          password: ******
        bootstrap: true
    bus:
      trace:
        enabled: true
server:
  port: ${PORT:1313}

