spring:
  application:
    name: config-server

  cloud:
    config:
      enabled: true
      server:
        git:
          uri: https://github.com/by-kifs/lasi/
          searchPaths: lasiGekenConfig
          username: by-kifs
          password: 9865mlr9865
        bootstrap: true
server:
  port: ${PORT:1313}

